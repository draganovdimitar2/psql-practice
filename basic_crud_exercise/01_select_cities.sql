/* 
To begin, the initial step is to generate a database known as softuni_management_db then query the data (from data.sql) into it
*/

-- Write a SQL query to retrieve all available information about the "cities", sorted by "id".

SELECT * FROM cities ORDER BY id ASC;