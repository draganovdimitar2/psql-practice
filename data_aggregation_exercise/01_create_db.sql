/* 
it is recommended to create a database named gringotts_db and open its query tool.
Open gringotts_data.sql which is inside data folder, import it's queries into the query tab of your database, and execute them.
*/

CREATE DATABASE gringotts_db;